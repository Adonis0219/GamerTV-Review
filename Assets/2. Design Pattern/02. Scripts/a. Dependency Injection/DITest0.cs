using UnityEngine;

public class DITest0 : MonoBehaviour
{
    // DI(Dependency Injection)
    // 의존성(의존 관계, 의존) : 객체가 자신의 기능을 수행하기 위해 필요한 다른 객체나 구성 요소

    // 의존성 주입
    // - 객체가 자신의 기능을 수행하기 위해 필요한 다른 객체나 구성 요소를 외부에서 제공받는 과정
    // - 컴포넌트를 드래그 드롭으로 스크립트에 연결하는 것도 넓은 의미에서 의존성 주입

    // 생성자 주입 : 객체가 생성될 때 생성자를 통해 필요한 의존성을 주입
    // 필드 주입 : 필드에 필요한 의존성을 직접 주입
    // 메서드 주입 : 메서드로 필요한 의존성 주입
    // 프로퍼티 주입 : 프로퍼티로 필요한 의존성을 주입
}
