using UnityEngine;

public class Lerp : MonoBehaviour
{
    // Lerp 함수
    // Lerp는 선형 보간법을 의미
    // Lerp 함수는 두 값 사이를 일정한 비율로 보간하여 새로운 값을 반환하는 함수입니다.
    // Lerp 함수는 일반적으로 다음과 같은 형태로 사용됩니다:
    // Mathf.Lerp(start, end, t)

    
}
